# Sentinel Python SDK

This directory contains the Apache-2.0 licensed Python SDK implementation.

Primary import surface:

```python
from sentinel_sdk import protect
```

Compatibility imports from `sentinel` continue to work via shims during the transition.
