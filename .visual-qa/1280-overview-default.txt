=== SIDEBAR ===
AGENT SECURITY
Varden
CONTROL PLANE
Overview
Rule Impact
Rules Workspace
Coverage Gaps
Web Shield
Authority & Provenance
Agent scope
All agents

Scopes charts, traces, and coverage. Rules workspace is unchanged.

Inspection mode
deep
fast

Lowest overhead. Runs classifiers and risk enrichment only when active policy requires them.

Refresh snapshot

=== MAIN ===
LIVE OPERATIONS
Authority & Provenance

Whether the causal chain was authorised to exercise the capability the agent attempted.

Posture: guarded
Events: 5
P95: 6.2 ms
AUTHORITY & PROVENANCE
Causal authority investigation
Overview
Incidents (5)
Attack Paths
Authority Map

Varden does not only ask whether an agent possesses a capability. It determines whether the causal chain influencing that action was authorised to exercise it.

Blocked incidents
3
Guarded actions stopped
Critical incidents
3
Highest severity events
Confused deputy
3
Untrusted influence on privilege
Exfiltration paths
1
Sensitive data toward public sinks
Cross-server flows
1
MCP trust boundary crossings
MCP trust changes
0
Fingerprint drift
GUARDED ACTIVITY
What Varden stopped
CRITICAL
BLOCKED
Private file access from untrusted MCP

Untrusted input attempted to use the agent's privileged access.

search.example / Search Web → demo-agent → Read file → notes.txt → BLOCKED
2m ago
CRITICAL
BLOCKED
Sensitive data headed toward a public destination

Sensitive data was headed toward a public destination.

github.com → demo-agent → Sensitive data → evil.example → BLOCKED
2m ago
ARCHITECTURAL EXPOSURE
3 potential authority compositions deserve attention

These are structural capability compositions — not detections that an attack occurred.

ARCHITECTURAL EXPOSURE
Untrusted input + sensitive read

Observed capabilities could permit untrusted input to influence private/secret filesystem access if authority controls were bypassed or incorrectly delegated.

ARCHITECTURAL EXPOSURE
Sensitive read + external write

Observed capabilities could permit sensitive filesystem data to reach public network destinations if authority controls were bypassed or incorrectly delegated.

ARCHITECTURAL EXPOSURE
Multiple MCP servers via agent

2 MCP servers are reachable through the agent; cross-server confused-deputy risk depends on trust boundaries.