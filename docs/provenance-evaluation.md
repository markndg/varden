# Provenance evaluation

```bash
varden provenance evaluate
varden provenance evaluate --json
```

Corpus: `varden/provenance/corpus/cases_v1.json`

Reports attack detection rate, benign allow rate, false positive/negative rates, median and p95 latency.

Do not invent numbers in docs — run the command and record the measured output.
